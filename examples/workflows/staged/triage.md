Review this issue and the repository's instructions:

{{machinist.prompt}}

Determine whether the requested behavior, acceptance criteria, and scope are
clear enough to implement. Do not change code or invent consequential decisions.
Return blocked with a specific question if information is missing. Return complete
if the issue is ready. Use the workflow result file described by the runtime.
The issue remains the shared source of requirements; later steps read it afresh.
